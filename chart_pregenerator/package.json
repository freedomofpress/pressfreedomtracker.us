{
  "name": "chart_pregenerator",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "build": "npx relative-deps && esbuild src/server.jsx --bundle --outfile=build/server.js --platform=node --loader:.node=copy",
    "start": "node ./build/server.js",
    "dev": "node dev.js"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.4.1",
    "esbuild": "0.18.10",
    "express": "^4.18.2",
    "node-fetch": "^3.3.1",
    "react": "^18.2.0",
    "react-animated-dataset": "^0.4.0",
    "react-dom": "^18.2.0",
    "tracker": "^0.0.0"
  },
  "devDependencies": {
    "chokidar": "^3.5.3",
    "nodemon": "^3.0.1",
    "relative-deps": "^1.0.7"
  },
  "relativeDependencies": {
    "tracker": ".."
  }
}
